# DailyTasksScreen — known bugs (TODO)

Строки 10295, 10297, 10309 (в оригинале App.jsx) ссылаются на
необъявленный идентификатор `lives`. Триггерится только в PLAYING-фазе
при ошибочном ответе когда skillLives > 0. Вероятно, остаток от старой
версии до перехода на skillLives map.

Не исправлялось при распиле — копирование 1:1.
Исправить отдельным bugfix-коммитом после завершения распила.
