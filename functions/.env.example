# Скопируй в `functions/.env` и заполни.
# Firebase CLI автоматически прокидывает эти переменные в runtime Gen 2 функций.
# После создания/изменения — повторить `firebase deploy --only functions`.

# Telegram bot для алёртов из awardWeeklyMedals (topOneCount ≥ 3).
TELEGRAM_TOKEN=
TELEGRAM_CHAT=
