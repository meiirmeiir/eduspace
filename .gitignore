node_modules/
dist/
.firebase/
.vercel/

# Secrets
.env
.env.*
!.env.example
fb_sa*
serviceAccount*
service-account*
*-credentials.json
*.pem
*.key

# Migration backups (may contain user data)
migration/backups/

# Playwright
playwright/.auth/
playwright-report/
test-results/

# CORS config (local infra setup, not source)
cors.json
