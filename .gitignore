node_modules/
dist/
.firebase/
.vercel/

# Secrets
.env
.env.*
!.env.example
fb_sa*
serviceAccount*
service-account*
*-credentials.json
*.pem
*.key

# Migration backups (may contain user data)
migration/backups/

# Playwright
playwright/.auth/
playwright-report/
test-results/

# CORS config (local infra setup, not source)
cors.json

# Cloud Functions
functions/node_modules/
functions/firebase-debug.log
functions/.firebase/
functions/.env

# Emulator/debug logs (транзиентные, не исходник)
firestore-debug.log
firebase-debug.log
.rec-profile/
_rec-raw/

# UI/UX Pro Max skill (external tooling, not app source)
.claude/skills/ui-ux-pro-max/

# Claude Code local settings (machine-specific, not source)
.claude/settings.local.json
